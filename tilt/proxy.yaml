apiVersion: apps/v1
kind: Deployment
metadata:
  name: fake-kube-proxy
spec:
  selector:
    matchLabels:
      app: fake-kube-proxy
  template:
    metadata:
      labels:
        app: fake-kube-proxy
    spec:
      containers:
      - name: snack
        image: busybox
        command: ["busybox", "httpd", "-f", "-p", "8000"]
        env:
        - name: IMAGE
          value: bazel/cmd/kube-proxy
        resources:
          requests:
            cpu: "10m"
